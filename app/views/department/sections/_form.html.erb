<%= form_with(model: [:department, section]) do |form| %>
  <% if section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(section.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
        <% section.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :trimester %>
    <%= form.number_field :trimester %>
  </div>

  <div class="mb-3">
    <%= form.label :shift, 'Turno', class: 'form-label' %>
    <%= form.select :shift, ['Mañana', 'Tarde', 'Fin de semana'], {}, class: 'form-select'  %>
  </div>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.text_field :course_id %>
  </div>

  <div class="field">
    <%= form.label :period_id %>
    <%= form.text_field :period_id %>
  </div>

  <div class="field">
    <%= form.label :career_id %>
    <%= form.text_field :career_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
