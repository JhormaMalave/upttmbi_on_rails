<%= form_with(model: [:department, section]) do |form| %>
  <% if section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(section.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
        <% section.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="mb-3">
    <%= form.label :name, 'Nombre', class: 'form-label' %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div class="mb-3">
    <%= form.label :trimester, 'Trimestre', class: 'form-label' %>
    <%= form.number_field :trimester, class: 'form-control' %>
  </div>

  <div class="mb-3">
    <%= form.label :shift, 'Turno', class: 'form-label' %>
    <%= form.select :shift, ['Mañana', 'Tarde', 'Fin de semana'], {}, class: 'form-control'  %>
  </div>

  <div class="mb-3">
    <%= form.label :course_id, 'Trayecto', class: 'form-label'  %>
    <%= form.collection_select :course_id, Course.all, :id, :name, {}, class: 'form-control' %>
  </div>

  <div class="mb-3">
    <%= form.label :period_id, 'Periodo', class: 'form-label'  %>
    <%= form.collection_select :period_id, Period.where(status: true), :id, :name, {}, class: 'form-control' %>
  </div>

  <div class="actions d-flex flex-row-reverse mb-3">
    <%= form.submit 'Enviar', class: 'btn btn-primary' %>
  </div>
<% end %>
